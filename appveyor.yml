build: off

clone_folder: c:\gopath\src\github.com\yawn\ykoath

environment:
  GOPATH: c:\gopath
  GO111MODULE: on

stack: go 1.12

test_script:
  - go test -coverprofile=coverage.txt -covermode=atomic -parallel 1 -race -tags ci
